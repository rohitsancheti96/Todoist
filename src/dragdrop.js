export const handleDragEnter = (e) => {
    e.preventDefault();
    e.stopPropagation();
};
export const handleDragLeave = (e) => {
    e.preventDefault();
    e.stopPropagation();
};
export const handleDragOver = (e) => {
    e.preventDefault();
    e.stopPropagation();
};
